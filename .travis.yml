language: python
dist: xenial
sudo: true
addons:
    postgresql: "9.6"

python:
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7"
  - "3.7-dev"
  - "3.8-dev"
  - "nightly"
  - "pypy3.5"

virtualenv:
  system_site_packages: false

services:
  - postgresql

before_install:
  - sudo apt-get update
  - sudo apt-get install graphviz

install:
  - pip install -U pip
  # only eggs: do an erreur with wheel
  - if [[ $TRAVIS_PYTHON_VERSION == '3.7' ]]; then pip install python_editor texttable PyYAML; fi
  - if [[ $TRAVIS_PYTHON_VERSION == '3.7-dev' ]]; then pip install python_editor texttable PyYAML; fi
  - if [[ $TRAVIS_PYTHON_VERSION == '3.8-dev' ]]; then pip install python_editor texttable PyYAML; fi
  - if [[ $TRAVIS_PYTHON_VERSION == 'nightly' ]]; then pip install python_editor texttable PyYAML; fi
  # only wheel
  - pip install wheel
  - pip install psycopg2 sqlalchemy  # for unittesting anyblok with postgres
  - pip install -U flake8 coverage pytest pytest-cov
  - pip install -U colour passlib furl phonenumbers pycountry  # special columns
  - pip install -U cryptography
  - pip install -U coveralls
  - python setup.py develop

env:
  global:
    - ANYBLOK_DATABASE_NAME=travis_ci_test
    - ANYBLOK_DATABASE_DRIVER=postgresql
    - ANYBLOK_DATABASE_USER=postgres

script:
  - flake8 anyblok
  - py.test --cov-report= --cov=anyblok anyblok/tests
  - psql -c 'drop database travis_ci_test;' -U postgres
  - anyblok_createdb --install-all-bloks
  - py.test --cov-report= --cov=anyblok anyblok/bloks
  
after_success:
  coveralls --data_file=.coverage --config_file=.coveragerc
