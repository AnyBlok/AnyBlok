name: Read the docs
on:
  push:
    branches:
      - main
      - master
    tags:
      - '**'
jobs:
  test:
    name: Read the docs
    runs-on: ubuntu-latest
    steps:
      - uses: readthedocs/actions/preview@v1
        with:
          project-slug: "anyblok"
